//! The `disk lockdown-das` implementation lives behind the daemon boundary.
//!
//! This marker file preserves the historical module path for tooling and
//! release notes; the CLI imports the typed request/response contract from
//! `dasobjectstore_daemon` and performs no managed-root mutation locally.
