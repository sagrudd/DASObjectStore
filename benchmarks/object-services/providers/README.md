# Providers

Provider directories contain Docker/Compose assets and provider-specific
bootstrap scripts.

Planned providers:

- `garage`
- `rustfs`

Keep provider configuration comparable. Provider-specific tuning must be
documented in the benchmark report before it is used for selection.
