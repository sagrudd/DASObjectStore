# REVIEWED TEMPLATE ONLY — not installed by package builders.
#
# Add an attended, one-use pair of opaque systemd credentials to the existing
# dasobjectstored.service only after the custody activation manifest is
# approved. Do not use Environment=, EnvironmentFile=, a registry, or an API
# to provide these values. The daemon accepts only files supplied by systemd in
# CREDENTIALS_DIRECTORY and consumes a hash-only marker before readback.
#
# Each credential file is UTF-8 key=value material with version, role,
# store_id, configuration_sha256, identity, aws_access_key_id, and
# aws_secret_access_key fields. Its opaque filename is referenced as
# systemd-credential://<opaque-name> by the attended retain request.
#
# Example shape only — replace both placeholder names and sources during the
# attended transaction; never commit real names, paths, access keys, or
# secrets into this template or a unit file.
[Service]
# LoadCredentialEncrypted=<opaque-writer-name>:<manager-held-encrypted-source>
# LoadCredentialEncrypted=<opaque-reader-name>:<manager-held-encrypted-source>
